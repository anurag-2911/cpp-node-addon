{
  "name": "cpp-node-addon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "start": "npm run build:dev && node index.js",
    "build": "node-gyp rebuild",
    "rebuild:dev": "node-gyp rebuild --debug",
    "build:dev": "node-gyp rebuild --debug",
    "clean": "node-gyp clean"
  },
  "author": "anurag kumar",  
  "devDependencies": {
    "node-gyp": "^3.7.0"
  },
  "dependencies": {
    "node-addon-api": "^1.3.0"
  }
}
